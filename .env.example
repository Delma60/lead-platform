# Database
DATABASE_URL=postgresql://user:password@region.neon.tech/dbname

# Authentication and encrypted settings bootstrap
ADMIN_PASSWORD=
AUTH_SECRET=
CRON_SECRET=

# Connected-app credentials are configured in /settings after login.
# Existing integration environment variables remain supported as migration fallbacks.

# Optional: Analytics
# NEXT_PUBLIC_GA_ID=
